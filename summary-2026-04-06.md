按 2026-04-06 当前仓库状态评估

项目定位
- `PaperCompass` 应按一个完整论文检索项目来理解，而不是按 Day1、Day2、Day3 三段彼此割裂的流水线来理解。
- 目前仓库中的 `day1_pipeline.py`、`day2_pipeline.py`、`day3_pipeline.py` 更适合作为内部实现演进痕迹；对外真正需要强调的是统一数据底座、统一检索能力、统一语义层和统一产品入口。

整体架构
- 数据层：本地数据源为 `data/arxiv_202502_cs_cl/` 下的论文 JSON，统一写入一个 SQLite 数据库。
- 索引层：同一个数据库中维护 `raw_papers`、`papers`、`paper_sections`、`paper_search_fts` 等表，承担结构化存储和 FTS 检索。
- 语义层：`paper_semantic_cards` 表承担语义卡片缓存，支持后续意图匹配、解释和语义增强。
- 产品层：命令行入口与 Streamlit 页面都应围绕统一项目接口工作，而不是直接绑定某个 day 文件。

当前完成情况
- 数据已完成统一入库：`day2_outputs/day2_full.db` 中当前有 `papers=1653`、`paper_sections=30487`、`paper_search_fts=1653`。
- 检索已具备完整基础能力：支持 FTS、标题 hint、作者粗匹配、方法名 / 数据集名短语级 exact match，以及 hybrid 合并。
- 语义层已落地：数据库中的 `paper_semantic_cards` 当前已有 100 条记录，`day3_outputs/` 中已有 Prompt、缓存策略、质量检查和稳定性报告。
- 前端已具备项目级可用性：当前已有 Streamlit 页面，可基于统一数据库执行查询并导出结果。
- 样例协议和 schema 已完成沉淀：`day1_outputs/` 中保留了样例记录、样例数据库和 schema 导出文件，可作为项目底层约定。

需要解释的地方
- 现实现路径已经从“直接读 tar 包”调整为“读取预解压 JSON 目录再入库”。这说明数据准备方式发生了变化，但不影响项目主体能力已经落地。
- 现有文件命名仍保留 `day1 / day2 / day3`，容易让人误以为项目是分阶段脚本集合；实际上它们共享同一个数据底座和产品目标。
- 如果继续沿用 day 级入口，会导致前端、后续功能和外部调用直接耦合内部实现细节，不利于项目长期演进。

当前剩余问题
- 项目级统一入口此前不够明确，外部调用容易直接落到 `day2_pipeline.py` 或 `day3_pipeline.py`。
- 工程分层还有继续收敛的空间，例如解析逻辑、检索逻辑、语义逻辑与产品入口之间还可以进一步解耦。
- 语义卡片当前完成的是“高价值优先”的 100 条覆盖，不是全库全量覆盖。

建议的整体处理方式
- 对外统一使用项目级入口组织功能，把 day 文件降为内部模块。
- 后续新增功能优先接入统一服务层，而不是继续在页面或脚本里直接依赖某一个 day 文件。
- 新能力应默认复用同一个 SQLite 数据底座、同一套项目状态管理和同一组对外命令，而不是新增新的分散式脚本链路。

总评
- 这个仓库已经不是“几个分散的实验脚本”，而是一个具备数据层、检索层、语义层和交互层的完整项目。
- 后续开发、汇报和答辩都应按“完整项目”表述，不应再把重点放在 Day1/Day2/Day3 的拆分叙事上。
