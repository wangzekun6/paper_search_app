# Python cache files
__pycache__/
*.pyc

# Local Streamlit runtime logs
tools/streamlit_stdout.log
tools/streamlit_stderr.log

# Large local database artifact that exceeds GitHub single-file limits
system_outputs/runtime/
system_outputs/cache/

# Extracted dataset restored from the bundled tarball; keep the expanded copy out of Git
data/

# Local plain tar archive is intentionally ignored because it exceeds GitHub limits.
bundled_data/*.tar
bundled_data/arxiv_202502_cs_cl.tar.gz
bundled_data/*.part*

# Temporary local archive probes
_tmp_arxiv_202502_cs_cl.zip
_tmp_arxiv_202502_cs_cl.tar

# Legacy archive DB snapshot is too large for GitHub regular pushes
archive/legacy_retrieval_assets/retrieval_reference.db
